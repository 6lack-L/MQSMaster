"""Placeholder. The VWAP execution algorithm lives in
``src/oms/sizing/vwap.py`` — this module exists only so an old import path
fails with a readable file rather than an empty one. Delete once nothing
references ``src.oms.order_types``.
"""
