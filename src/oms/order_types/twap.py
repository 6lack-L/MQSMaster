"""Placeholder. The TWAP execution algorithm lives in
``src/oms/sizing/twap.py`` — this module exists only so an old import path
fails with a readable file rather than an empty one. Delete once nothing
references ``src.oms.order_types``.
"""
